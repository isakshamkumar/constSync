// Example vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
    // ... other configurations
    build: {
        outDir: 'dist', // Check this setting, it specifies the output directory
    },
});
